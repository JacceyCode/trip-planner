const GlobalSpinner = () => {
  return (
    <div className="glassmorphism fixed inset-0 z-50 flex items-center justify-center">
      <div className="loader" />
    </div>
  );
};

export default GlobalSpinner;
